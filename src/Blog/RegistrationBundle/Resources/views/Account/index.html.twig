<h1>click email for password</h1>
<ul>
    {% for user in users %}
        <li><a href="{{ path('account_get') }}/{{ user.id }}">{{ user.email }}</a></li>
    {% endfor %}
</ul>

<a href="{{ path('account_create') }}">Add a user</a>